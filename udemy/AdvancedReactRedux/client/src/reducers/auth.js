/* eslint-disable import/no-anonymous-default-export */
const INITIAL_STATE = {
    authenticated: '',
    errorMessage: ''
};

export default function(state = INITIAL_STATE, action) {
    return state;
}