name: CI

on:
  pull_request:
    branches: [develop]
    paths:
      - .github/**
      - 'udemy/AdvancedReactRedux/**'
  push:
    branches: [develop]
    paths:
      - .github/**
      - 'udemy/AdvancedReactRedux/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Use NodeJs
        uses: actions/setup-node@v1
        with:
          node-version: "12.x"
      - run: pwd
      - run: cd udemy/AdvancedReactRedux/testing/ && npm ci
      - run: cd udemy/AdvancedReactRedux/testing/ && npm run format:check
      - run: cd udemy/AdvancedReactRedux/testing/ && npm test -- --coverage
        env:
          CI: true
      - name: Build Project
        if: github.event_name == 'push'
        run: cd udemy/AdvancedReactRedux/testing/ && npm run build
      - name: Deploy to Staging
        if: github.event_name == 'push'
        run: cd udemy/AdvancedReactRedux/testing/ && npx surge --project ./build --domain desertfoxdev.surge.sh
        env:
          SURGE_LOGIN: ${{ secrets.SURGE_LOGIN }}
          SURGE_TOKEN: ${{ secrets.SURGE_TOKEN }}

            